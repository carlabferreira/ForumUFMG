import { db } from "../connect.js";

//app.get("/busca", (req, res) => {
//    const titulo = req.body.titulo;
//    const tags = req.body.tags;
//    const categoria = req.body.categoria;
//
//    q = db.query("SELECT * FROM topicos WHERE titulo = ? )
//};